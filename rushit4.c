#include<stdio.h>
#include<float.h>
#include<limits.h>
void main()
{
    printf("size of int : %zu",size of (int) );
    printf("size of long int : %zu",size of (long));
    printf("size of char : %zu", size of (char));
    printf("size of float : %zu", size of (float));
    printf("size of ")
}
